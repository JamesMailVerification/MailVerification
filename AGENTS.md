# Codex Project Instructions

## 1. 프로젝트 기준 문서

이 프로젝트의 제품 요구사항 기준 문서는 다음 파일이다.

- `docs/PRD.md`

모든 작업을 시작하기 전에 반드시 다음 순서로 진행한다.

1. 프로젝트 루트의 `AGENTS.md`를 읽는다.
2. `docs/PRD.md`를 읽는다.
3. 현재 요청과 PRD의 목표, MVP 범위, User Flow 및 기능 요구사항이 일치하는지 확인한다.
4. 관련 기존 코드를 확인한다.
5. 작업 범위와 제외 범위를 구분한 후 작업한다.

## 2. 요구사항 우선순위

요구사항이 충돌할 경우 다음 순서를 따른다.

1. 사용자가 현재 작업에서 명시적으로 지시한 최신 요구사항
2. `docs/PRD.md`
3. 기존 코드의 동작
4. README 및 기타 문서

현재 사용자 요청과 PRD가 충돌하면 임의로 판단하여 구현하지 않는다.

다음 내용을 사용자에게 명확히 보고한다.

- 충돌하는 PRD 항목
- 현재 요청 내용
- 변경 시 영향을 받는 기능

## 3. MVP 핵심 범위

현재 MVP의 핵심 범위는 다음과 같다.

- Gmail 또는 Outlook 계정 연결
- 이메일에서 일정과 답변 기한 추출
- 추출한 일정 후보를 사용자에게 표시
- 사용자가 확인하고 선택한 일정만 캘린더에 등록
- 마감 3일 전부터 매일 오전 9시에 알림 제공
- 오늘 처리할 일정과 답변 업무를 Dashboard에 표시

## 4. MVP 제외 범위

별도의 PRD 변경 지시가 없는 한 다음 기능은 구현하지 않는다.

- 이메일 자동 발송
- 사용자의 승인 없는 자동 회신
- AI 답장 작성
- Slack 연동
- Microsoft Teams 연동
- 팀 협업 및 권한 관리
- 회의록 자동 작성
- 일정 우선순위 자동 결정
- 사용자의 확인 없는 일정 자동 등록

## 5. 구현 원칙

- PRD에 없는 기능을 임의로 추가하지 않는다.
- 사용자의 확인 없이 일정을 확정하거나 캘린더에 등록하지 않는다.
- 날짜나 시간이 명확하지 않으면 임의로 추정하지 않는다.
- 불명확한 항목은 `[확인 필요]` 상태로 표시한다.
- 이메일 제목, 발신자, 일정 종류, 날짜, 시간, 답변 기한 및 원본 메일 링크를 구분하여 처리한다.
- 메일 원문은 서비스 제공에 필요한 범위를 넘어 저장하지 않는다.
- 비밀번호, OAuth 토큰, API 키를 소스코드에 직접 저장하지 않는다.
- 인증 정보는 환경변수 또는 안전한 비밀 관리 방식을 사용한다.
- 오류, 빈 결과, 로딩 및 인증 실패 상태를 처리한다.
- 화면은 비개발자도 이해할 수 있도록 단순하고 명확하게 구성한다.
- 기존 기능을 불필요하게 변경하지 않는다.

## 6. 작업 전 확인 사항

코드를 수정하기 전에 다음 내용을 짧게 정리한다.

- 이번 작업과 관련된 PRD 항목
- 구현할 기능
- 수정할 것으로 예상되는 파일
- 이번 작업에서 제외할 기능
- 확인이 필요한 사항

사용자가 명시적으로 바로 구현하라고 지시한 경우에는 불필요한 승인을 다시 요청하지 말고 합리적인 범위에서 작업한다.

## 7. 작업 완료 전 검증

작업을 완료하기 전에 다음을 확인한다.

- 관련 PRD 요구사항을 충족했는가?
- MVP 제외 기능이 들어가지 않았는가?
- 사용자 확인 없이 일정이 등록되지 않는가?
- 불명확한 날짜와 시간이 `[확인 필요]`로 처리되는가?
- 오류 및 로딩 상태가 처리되는가?
- 인증 정보가 코드에 노출되지 않았는가?
- 기존 기능이 손상되지 않았는가?
- 가능한 테스트, 타입 검사 및 빌드를 실행했는가?
- 문서와 실제 구현이 일치하는가?

## 8. 작업 완료 보고 형식

작업이 끝나면 다음 형식으로 보고한다.

### 참조한 PRD 항목
- 해당 요구사항

### 구현한 내용
- 실제 구현 기능

### 변경한 파일
- 파일 경로와 변경 내용

### 검증 결과
- 테스트
- 타입 검사
- 빌드
- 수동 확인

### 남은 문제
- 미완료 사항
- `[확인 필요]` 사항

### PRD와 다른 부분
- 차이가 없으면 `없음`으로 표시
